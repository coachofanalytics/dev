# Core Django and Web Framework
Django==3.2.6
gunicorn==20.1.0
whitenoise==5.2.0

# Database
psycopg2==2.9.5
dj-database-url==0.5.0

# Django Extensions
django-crispy-forms==1.12.0
django-countries==7.6.1
django-filter==2.4.0
django-mptt==0.13.4
django-storages==1.11.1
django-timezone-field==5.0
django-heroku==0.3.1

# Authentication
django-allauth==0.58.2
django-otp==1.3.0

# Celery for Background Tasks
celery==5.5.1
django-celery-beat==2.4.0
django-celery-results==2.4.0
redis==4.3.1

# Core Python Libraries
python-dotenv==0.20.0
requests==2.31.0
Pillow==11.1.0
python-dateutil==2.9.0
pytz==2025.2

# Security
cryptography==41.0.5
PyJWT==2.8.0
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0
google-api-python-client==2.108.0

# Data Processing & Web Scraping (for OptionPlay integration)
# Note: Required for real-time position fetching from OptionPlay
pandas==2.2.3  # 73MB - Required for OptionPlay scraper
beautifulsoup4==4.12.2  # HTML parsing for web scraping
lxml==4.9.3  # Fast XML/HTML parser
playwright==1.40.0  # Browser automation for OptionPlay scraping
yfinance==0.2.32  # FREE stock data - RSI, MA, volume for technical confirmation

# File Processing
python-docx==0.8.11
openpyxl==3.1.5
PyPDF2==3.0.1

# HTTP and API
httpx==0.26.0
aiohttp==3.10.5

# WhatsApp/Telegram Notifications (Phase 3 - AI Alerts)
twilio==8.10.0  # WhatsApp Business API via Twilio

# Utilities
click==8.1.3
tqdm==4.64.1
packaging==23.2
qrcode==7.4.2
pillow==11.1.0  # Required for qrcode (already included above)

# Development and Testing (only for development)
coverage==7.10.6
psutil==7.1.0

# Note: Removed heavy packages that may not be essential:
# - chromedriver-py==140.0.7339.82 (85MB) - use system ChromeDriver
# - google-api-python-client (63MB) - use specific API clients if needed
# - botocore (58MB) - use boto3 selectively
# - grpcio (32MB) - only if using gRPC
# - lxml (20MB) - only if parsing XML
# - sqlalchemy (18MB) - Django ORM is usually sufficient
# - langchain packages (30MB+) - only if using AI features
# - selenium (if not needed for production)
# - PyAutoGUI and related automation packages
# - PyInstaller (build tool, not needed in production)
# - Various Google Cloud packages (use only if needed)

django-mathfilters==1.0.0

httplib2==0.21.0
